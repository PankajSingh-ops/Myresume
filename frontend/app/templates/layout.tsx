export const metadata = {
    title: 'Resume Templates | Resume Builder',
    description: 'Browse our collection of professional, ATS-friendly resume templates.',
};

export default function TemplatesLayout({ children }: { children: React.ReactNode }) {
    return children;
}
